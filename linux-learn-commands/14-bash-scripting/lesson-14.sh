bash filename                    # Executes the commands in the specified script file using the Bash shell.

# Example:
bash script.sh                   # Runs the commands in "script.sh" using the Bash shell.
